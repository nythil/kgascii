PROJECT(TOOLS)

SET(Boost_USE_STATIC_LIBS        ON)
SET(Boost_USE_MULTITHREADED      ON)
SET(Boost_USE_STATIC_RUNTIME    OFF)
FIND_PACKAGE(Boost 1.46.0 COMPONENTS filesystem system program_options REQUIRED)
FIND_PACKAGE(Freetype REQUIRED)
FIND_PACKAGE(Eigen3 REQUIRED)
FIND_PACKAGE(OpenCV REQUIRED)

#INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIRS})
#INCLUDE_DIRECTORIES(${FREETYPE_INCLUDE_DIR_ft2build})
#INCLUDE_DIRECTORIES(${EIGEN3_INCLUDE_DIR})
#INCLUDE_DIRECTORIES(${OpenCV_INCLUDE_DIR})
#INCLUDE_DIRECTORIES(${KGASCII_SOURCE_DIR})
#ADD_EXECUTABLE(tools ${tools_SRCS})
#TARGET_LINK_LIBRARIES(tools kgascii)
#TARGET_LINK_LIBRARIES(tools ${FREETYPE_LIBRARY})
#TARGET_LINK_LIBRARIES(tools ${Boost_LIBRARIES})
#TARGET_LINK_LIBRARIES(tools ${OpenCV_LIBS})

ADD_SUBDIRECTORY(dumpfont)
ADD_SUBDIRECTORY(extractfont)
ADD_SUBDIRECTORY(dsc2img)

